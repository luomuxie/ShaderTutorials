线性RGB更接近真实世界中光的行为，更准确地计算光照和阴影。在这个空间中混合颜色更自然，结果也更可预测。进行高级图形效果（比如HDR）时，线性RGB让处理变得更简单，效果也更好。
大多数设备和图片格式使用的是非线性色彩空间，比如sRGB，因为它们可以在有限的存储空间内更有效地表示颜色。

简单来说，OpenGL在内部使用线性RGB是为了让图形处理更接近物理真实，以及让渲染效果更好。但最终显示时，通常会转换成sRGB，因为这样能更好地匹配现有的显示技术和设备，同时节省存储空间。